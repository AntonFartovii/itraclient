import React from 'react';
import {Button, Form} from "react-bootstrap";

const FormPropCheckbox = ({}) => {
    return (

            <Form.Check type="checkbox" label="Check me out" />

    );
};

export default FormPropCheckbox;